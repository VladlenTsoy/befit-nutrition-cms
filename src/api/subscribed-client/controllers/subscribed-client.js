'use strict';

/**
 * subscribed-client controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::subscribed-client.subscribed-client');
